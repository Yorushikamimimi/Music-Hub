# 项目进度跟踪（Progress）

> 更新时间：2026-03-21（Asia/Shanghai）

## 里程碑状态
- 当前阶段：`MVP Stabilization`（MVP 稳定化阶段）
  - 中文解释：核心能力可用，正在做下线前收口和安全整改。

## 已完成
1. 项目清理
- 删除 `venv`、缓存目录、历史日志、空目录等非必要内容。

2. 合规与功能下线
- 删除评论/留言全链路（后端路由 + 前端页面）。
- 删除头像悬停音乐/弹幕功能，并移除本地 MP3 文件。
- 关闭头像上传（前端入口 + 后端处理均已关闭）。

3. 安全配置加固
- `config.py` 改为强制读取环境变量。
- `SECRET_KEY` 增加强度校验。
- `netease_spider.py` 去除硬编码数据库密码，改为环境变量。

4. 可运行性验证
- 执行 `python -m py_compile app.py routes/main.py models.py utils.py config.py netease_spider.py` 通过。

## 当前可交付能力
- 首页歌曲展示 + 每日推荐。
- 搜索筛选与排序。
- 收藏心愿单（浏览器本地持久化）。
- 关于页展示（不含上传与评论）。

## 未完成 / 可继续探索
1. 文案与编码治理
- 修复模板中的乱码文本，统一 UTF-8 文案。

2. 数据与脚本治理
- 清理 `netease_spider.py` 的历史脏注释与乱码，拆分“建表/抓取/导入”职责。

3. 工程化补强
- 增加基础测试（路由可用性、模板渲染、配置校验）。
- 增加部署前检查清单与最小监控。

## 当前风险
- `.env` 中数据库密码仍为历史值，建议尽快完成数据库侧密码轮换并同步更新。
